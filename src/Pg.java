import java.util.Arrays;
import java.util.Scanner;

public class Pg implements Run {
    @Override
    public void run() {
        Scanner scan = new Scanner(System.in);
        int A = scan.nextInt();
        int B = scan.nextInt();
        int V = scan.nextInt();
        int x;

        (A-B) * X + A >= V
                (A * X) + A - (B * X) >= V
                A (X + 1) - ()
  sa  }
}
